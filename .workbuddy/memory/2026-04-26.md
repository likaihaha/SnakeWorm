2026-04-26 02:01 - 吃豆人嘴型效果实现
- 在 prototype_v0.11_newexperiment.html 中实现吃豆人嘴型动画
- 头部前方三角形嘴巴，随时间一张一合（周期约0.3秒）
- 嘴巴开合角度：0.15 ~ 0.8 弧度循环
- 嘴巴颜色为黑色（背景色），轮廓为蚯蚓本体颜色
- 移除了传统眼睛，改用嘴部代替
- 删除了 prototype_v0.10_pacmanmouth_experiment.html（空壳文件）

2026-04-26 02:07 - 吃豆人嘴型优化（arc 绘制）
- 新增 drawPacmanHead() 方法，使用 Canvas 的 arc() 绘制带缺口的圆形
- 嘴巴开口角度根据速度动态调整：mouthOpen = Math.min(0.8, 0.15 + this.speed * 0.08)
- 速度越快，嘴巴张得越大
- 嘴巴方向跟随移动角度（angle = Math.atan2(velocity.y, velocity.x)）
- 添加一张一合动画效果（周期约0.3秒），在速度基础上叠加动画因子
- 修改 draw() 方法：头部段优先使用 drawPacmanHead() 绘制吃豆人形状
- 身体段保持圆形绘制
- 保留了头部/尾巴区域标识功能

2026-04-26 02:15 - 5 项优化改进
- 1. 鼠标指针改为 18px 圆环：CSS cursor:none 隐藏默认光标，Game.drawCustomCursor() 绘制白色圆环（半径 9px，线宽 2px）
- 2. 速度限制防冲过：移动步长计算后检查 distToTarget，如果会冲过目标则直接停在 target 位置，解决 direction 反复反转问题
- 3. 嘴巴去掉发光效果：shadowBlur = 0
- 4. 嘴巴颜色与身体一致：使用 this.hexToRgba(this.color, 0.8)，透明度 0.8 与身体段亮度一致
- 5. 嘴巴朝向基于 head 到 segments[1] 的向量：dir = headPos.sub(this.segments[1])，向量连续变化，嘴巴朝向更平滑，始终指向实际移动方向

2026-04-26 02:22 - 修复头部抖动和添加眼睛
- 修复头部抖动：当 distanceToTarget < 15 时直接停在目标位置，不更新 velocity
- 添加眼睛：在 drawPacmanHead() 内部调用 drawEyes()，绘制黑色眼睛
- 眼睛位置：在头部前方嘴巴两侧（eyeOffset=5 向前，eyeSpread=3 左右分开）
- 眼睛颜色：纯黑色（#000），eyeRadius=2.5
- 眼睛绘制模式：使用 source-over 而不是 Screen，否则黑色眼睛会消失
- 嘴部张合动画：周期约 0.3 秒，animPhase = (Math.sin(performance.now() / 150) + 1) / 2
- 嘴巴开口角度：mouthOpen = Math.min(0.8, 0.15 + this.speed * 0.08)，速度越快开口越大

2026-04-26 02:26 - 加大嘴巴动画和头部半径
- 头部半径加大：drawRadius = radius + 2（与虚线圆环大小一致）
- 嘴巴开口角度加大：mouthOpen = Math.min(1.5, 0.3 + this.speed * 0.1)（基础 0.3，最大 1.5）
- 眼睛位置动态调整：eyeOffset = drawRadius * 0.6，eyeSpread = drawRadius * 0.5（相对于头部半径）
- 嘴巴动画更明显：动画因子放大开口，张合幅度更大

2026-04-26 02:29 - 保存 v0.12 备份
- 保存当前版本为 prototype_v0.12_pacman_with_eye.html
- 当前功能：吃豆人嘴型 + 单只头顶眼睛 + 嘴巴张合动画 + 18px 鼠标圆环 + 速度限制防冲过
- 等待下次继续修改
