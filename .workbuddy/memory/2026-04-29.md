# 2026-04-29 日志

## 初始身份建立
- 完成 Bootstrap 身份引导
- 用户称呼：表哥，住在珠海
- 我的名字：八弟，身份是 AI 表弟
- 风格：中文交流，随意务实，不废话
- Emoji：🐍
- 已更新 SOUL.md / IDENTITY.md / USER.md，删除 BOOTSTRAP.md

## v1.19 代码审查优化
- 根据 REVIEW.md 优先修复顺序继续优化
- **SpatialGrid 接入碰撞检测**：此前 SpatialGrid 类已实现但从未接入实际碰撞。修改 checkTailBite/checkNeckBite/checkOtherWormCollision 添加可选 spatialGrid 参数，Game.update 中每帧调用 populateSpatialGrid() 填充
- **SpatialGrid.clear() 优化**：复用数组（length=0）减少 GC
- **Magic Number 收敛**：移动系统常量 STOP_DISTANCE(15)、LERP_STOP_THRESHOLD(1)、LERP_MOVE_FACTOR(0.15)、LERP_BODY_FACTOR(0.3) 提取到 CONFIG
- **restart() 清理**：补充 enemies/particles/floatingTexts/bullets/slowedFoods/slowedWorms/enemySpawnTimer/foodRespawnTimers 的重置
- **死亡对话框修复**：从 location.reload() 改为 game.restart()，避免重新加载整个页面
- **REVIEW.md 更新**：重写为 v1.19 跟踪版，标记所有已完成和待完成项
- 调研发现：hexToRgba 已在之前版本合并为1个定义；Worm.update 已拆分7个子方法；成熟逻辑已提取；ENEMY_STATE/GAME_STATE 已实现；网格预渲染已实现；shadowBlur 已优化
- 语法检查通过，已 commit + push + tag v1.19

## 项目文件整理
- 创建 BAK/ 目录：归档旧原型(14个prototype_*.html)、旧版本压缩包(6个MelodyWorm_v*.zip)、旧设计文档和审查报告(4个.md)
- 创建 Music/ 目录：归档音乐相关文件(MIDI文件3个、MIDI生成脚本2个、音乐JS 3个、音乐测试页、封面图片2个)
- 整理后根目录只剩 index.html + BAK/ + Music/

## 全面代码审查
- 以资深游戏架构师角度审查 index.html (v1.19, 6316行)
- 总体评分 7.1/10，音频系统和游戏循环最好(9/10)，内存管理和可维护性最弱(5/10)
- 关键发现：Vector临时对象GC压力大、碰撞检测冗余Set创建、Game/Worm类过大(2000+/1600+行)、状态管理冗余字段
- 亮点：空间分区碰撞检测、固定步长游戏循环、Web Audio合成钢琴、移动端全面支持
- 报告输出到 code_review_report.md

## v1.20 Vector就地修改优化
- 根据审查报告开始逐项优化，共21个任务
- 任务#1完成：Vector类新增addSelf/subSelf/multSelf/normalizeSelf/set方法
- 优化8个热路径为纯数学运算：updateBodyFollowing、移动逻辑、击退效果、身体跟随、磁力吸引、子弹碰撞、碰撞hitDir、fireBullet
- 预期每帧减少数百个临时Vector对象，降低GC压力
- 已commit+push+tag v1.20

## v1.24 Particle对象池
- 任务#4完成：Particle类新增静态_pool/acquire/release/_reset方法
- 所有外部new Particle()替换为Particle.acquire()（约13处）
- 粒子死亡时Particle.release()回收到池（2处回收点：常规粒子消亡+restart清空）
- restart()中遍历particles数组逐个release后清空
- 已commit+push+tag v1.24
- v1.21-v1.23也在此session完成（碰撞Set缓存、DOM引用缓存、SpatialGrid.query复用）

## v1.25 shadowBlur全面替换
- 任务#6完成：9处shadowBlur全部替换为径向渐变
- 新增drawGlow()/drawTextGlow()公共工具函数
- 浮动文字用偏移文字替代，宝珠/虫虫/敌人用径向渐变光晕
- 清理死代码（head段无用shadowBlur=0）
- 已commit+push+tag v1.25

## v1.26 splice改紧凑过滤模式
- 任务#7完成：热路径splice替换为紧凑过滤(write-pointer)模式
- 优化数组：particles、floatingTexts、foods、bullets、brokenTails、deadBodies、enemies、iceOverlays
- bullets循环重构：4个splice点合并为1个hit标志位判断
- 保留splice：parallel arrays(segments+velocities, blueStrengths)、小数组(growing/shrinking)、单次删除
- 已commit+push+tag v1.26

## v1.27-v1.30 代码质量优化（4个版本一起推送）
- v1.27: 统一状态字段 — 删除isRunning/isPaused/isGameOver，全部改用this.state === GAME_STATE.*
- v1.28: 游戏结束逻辑集中管理 — 所有玩家死亡路径统一走gameOver()入口，支持预创建尸体+防重复触发
- v1.29: 消除魔法数字 — Enemy/Particle/Bullet类20+硬编码数字提取到CONFIG命名常量
- v1.30: 全局错误边界 — loop()添加try-catch保证帧循环不中断+window.onerror防静默卡死
- 一起打标签推送

## v1.31 五项体验修复
- 文字模糊：drawTextGlow偏移量从0.3→0.15，透明度从0.3→0.15
- 幼体发光特效：drawGlow中心alpha从0.5→0.25，断尾glowSize从15→8
- 敌人卡顿：takeDamage添加isDying守卫+updateEnemies碰撞检测跳过isDying敌人
- 橙色宝珠光效：去掉磁力脉冲drawGlow实心填充，只保留圆环
- 灰色尾巴：checkJuvenileEatShrinkingSegments从只吃父代改为所有幼体吃所有灰色尾巴；AI追逐目标同步放开
- 已commit+push+tag v1.31

## v1.32 渲染热路径优化
- draw()中8处forEach→for循环(brokenTails/enemies/deadBodies/foods/particles/bullets/worms/floatingTexts)
- update()中foods.forEach→for循环
- 节奏宝珠生成：foods.filter().length→手动计数，消除临时数组分配（每帧每种宝珠类型少一个临时数组）
- 波动动画：waveOffsets从new Vector数组→Float32Array，normalize()→直接数学计算
- 缩小段颜色：每帧parseInt→constructor缓存_colorR/G/B/_grayColor
- 已commit+push+tag v1.32

## v1.34 断尾幼体去掉光圈缩放+成年给无敌防碰死
- Bug1: BrokenTail.draw()有大幅发光脉冲效果(glowScale最高到16)，去掉glowScale/drawGlow相关代码
- Bug2: 幼体吃够宝珠成年时(matureJuvenile)isJuvenile变false但没给无敌时间，下一帧玩家碰到刚成年的虫直接死 → 成年时给invincibleTimer=10秒
- 已commit+push+tag v1.34

## v1.35 FloatingText对象池+热路径临时Vector消除
- FloatingText静态对象池：_pool/acquire/release/_reset，18处new改为acquire
- FloatingText.update()就地修改pos，消除每帧2个临时Vector
- Bullet.update()就地修改pos，消除每帧2个临时Vector
- Enemy.update()/updateDying()就地修改，消除每帧N个临时Vector
- Enemy死亡下沉身体跟随改为纯标量运算，消除每段3个临时Vector
- Food颜色RGB缓存(_cr/_cg/_cb)，消除morphing阶段每帧parseInt
- 修复FloatingText.acquire()池子为空时无限递归bug（应该是new而不是递归acquire）
- 已commit+push+tag v1.35
