# 2026-04-30 日志

## 审查报告任务梳理
- 梳理了 code_review_report.md 中剩余未完成任务
- 高优先级4项和中优先级4项全部在 v1.20~v1.35 中完成
- 剩余：事件监听器清理、globalCompositeOperation优化、数组上限保护、音频节点池化、模块化拆分、Game/Worm类拆分、JSDoc、单元测试等

## 目录整理推送
- 3715883: 整理项目目录结构 - 原型文件归档到BAK/，音乐素材归档到Music/

## 单文件拆分计划
- 表哥让 Claude Code 在拆 v1.33 之前的版本为多文件模块（ES Modules）
- 拆好后搬过来，由八弟 review + 补上 v1.34~v1.35 的改动
- 注意事项：文件名全小写、循环依赖处理、全局变量 export/import

## v1.35 发布 - 多文件ESM重构+审查修复（e7709cd）
- 单文件6600行拆分为14个ES模块，index.html改为`<script type="module">`
- 修复审查6个问题：
  1. 删除worm.js误搬的spawnWorm()死代码（引用不存在的this.newColor）
  2. FloatingText接入对象池（static _pool/acquire/release/_reset），game.js 14处+enemy.js 4处改为acquire
  3. 热路径优化：Bullet/FloatingText/Particle/Enemy.update中消除临时Vector，改为标量就地运算
  4. Food颜色缓存（_cr/_cg/_cb），morphing阶段不再每帧parseInt
  5. 删除entities.js重复drawTextGlow定义，改为import from utils.js
  6. 版本号统一v1.35（title/h1/startScreen三处）
- 同时修复enemy.js击退效果的临时Vector（knockback就地修改）
- 补修：enemy.js漏import Particle（consumeFeedingTarget crash）→ 4784029
- 补修：worm.js漏import Food和Particle（updateShrinkingSegments crash）→ fd37839

## v1.36 排行榜系统（7b9f75d）
- 新建 src/leaderboard.js - Leaderboard类，LocalStorage存储（key: snakeworm_leaderboard）
- 游戏结束死亡对话框改为排行榜对话框：显示成绩+输入名字+提交+查看排行榜
- 开始界面新增"🏆 排行榜"按钮，不进游戏也能看
- 排行榜字段：名字、长度、分数、孩子、日期
- 点击表头排序（再点反转），支持中文排序（日期字段用localeCompare）
- 最多100条记录，按分数降序存储
- version: v1.36

## v1.37 排行榜返回修复
- 修复：死亡后填排行榜点"返回"只关闭对话框停在游戏画面，不回到开始菜单
- leaderboard.js: show()新增onClose回调参数，"返回"按钮调用overlay.remove()+onClose()
- game.js: 新增backToStartScreen()方法 — 重置state=IDLE、清理游戏对象、显示startScreen+按钮
- game.js: showPlayerDeathDialog()传入onClose回调
- version: v1.37

## v1.38 排行榜管理员模式（c6f6fdb）
- 排行榜标题栏右侧添加⚙️按钮，点击弹出管理员登录对话框
- 用户名密码用SHA-256哈希比对（crypto.subtle.digest），不存储明文
- 登录成功后每条记录右侧出现🗑️删除按钮，确认后删除
- 表头自动扩展"操作"列，退出管理模式自动收起
- 管理模式状态存储在Leaderboard._isAdmin静态属性
- version: v1.38

## v1.39 ESC暂停复用开始界面（fe4bd5a）
- 按ESC暂停时不再显示独立pauseScreen，改为复用startScreen
- 暂停时startBtn文字改为"继续游戏"，点击可继续游戏
- 再按ESC也能恢复游戏（原有逻辑保留）
- backToStartScreen()中重置startBtn文字为"开始游戏"
- main.js: startBtn点击判断PAUSED→resumeGame，否则→startGame
- 隐藏的pauseScreen保留（不再显示但不删除DOM）
- version: v1.39
